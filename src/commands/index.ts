import * as ping from "./ping";
import * as rcon from "./rcon";
import * as add from "./add";
import * as list from "./list";
import * as remove from "./remove";
import * as died from "./died";

export const commands = {
  ping,
  rcon,
  add,
  list,
  remove,
  died
};
